
{%- doc -%}
  Renders a header menu block.

  @param {string} [variant] - What version of the menu to render. Defaults to header menu.
  @param {string} [transparent] - Show a transparent menu
{%- enddoc -%}

{% liquid
  assign block_settings = block.settings
  assign effective_submenu_color = block_settings.background_color | default: settings.page_background_color
%}

{% case variant %}
  {% when 'mobile' %}
    <div
      class="header__drawer"
      ref="headerDrawerContainer"
      data-hydration-key="header-drawer-mobile"
      {{ block.shopify_attributes }}
    >
      {% render 'header-drawer',
        linklist: block_settings.menu,
        data_header_drawer_type: 'mobile-drawer',
        block: block,
        section: section
      %}
    </div>

  {% when 'navigation_bar' %}
    {% if block_settings.navigation_bar == true %}
      {% render 'contrast-override',
        background_color: block_settings.background_color_navigation_bar,
        text_color: block_settings.text_color_navigation_bar,
        section_id: block.id
      %}
      <div
        class="menu-list menu-list--mobile{% if transparent == blank %} color-custom-{{ block.id }}{% endif %}"
        style="--menu-horizontal-gap: 15px; --mobile-nav-margin: auto;"
        data-hydration-key="header-menu-mobile-navigation-bar"
      >
        <div class="menu-list__scroll-container">
          <ul
            class="menu-list__list list-unstyled"
            role="list"
          >
            {% render 'primary-nav', variant: 'mobile_bar', menu_settings: block_settings %}
          </ul>
        </div>
      </div>
    {% endif %}
  {% else %}
    {% liquid
      assign block_settings = block.settings

      render 'contrast-override', background_color: block_settings.background_color, text_color: block_settings.text_color, section_id: block.id

      assign background_color_classes = ''
      assign background_color_setting_id = 'background_color_' | append: section.settings.menu_row
      assign current_background_color = block_settings.background_color | default: settings.page_background_color
      assign parent_background_color = section.settings[background_color_setting_id] | default: settings.page_background_color

      if parent_background_color != current_background_color or block_settings.text_color != blank
        assign background_color_classes = ' color-custom-' | append: block.id
      endif

      # Check if header and menu colors match. This is used to apply different padding styles in css
      if parent_background_color.rgb == current_background_color.rgb
        assign background_color_classes = background_color_classes | append: ' background-matches-parent'
      endif
    %}

    {% capture children %}
      {% render 'primary-nav', variant: 'desktop', menu_settings: block_settings %}
      <li
        class="menu-list__list-item"
        role="presentation"
        slot="more"
        on:focus="/activate"
        on:blur="/deactivate"
        on:pointerenter="/activate"
        on:pointerleave="/deactivate"
      >
        <button
          role="listitem"
          class="menu-list__link button-unstyled"
          aria-haspopup="true"
          aria-expanded="false"
          ref="menuitem"
        >
          <span class="menu-list__link-title">{{ 'actions.more' | t }}</span>
        </button>
      </li>
    {% endcapture %}

    <header-menu
      ref="headerMenu"
      class="header-menu"
      {{ block.shopify_attributes }}
      data-hydration-key="header-menu"
    >
      <div class="header-menu__inner">
        <nav
          class="menu-list"
          aria-label="{{ 'accessibility.header_navigation_label' | t }}"
          style="{% render 'menu-font-styles', settings: block_settings, menu_type: 'mega_menu' %}"
        >
          {% assign class = 'overflow-menu' | append: background_color_classes %}
          {% render 'overflow-list',
            class: class,
            ref: 'overflowMenu',
            children: children,
            minimum-items: 2,
            data-testid: 'header-menu-overflow-list',
            attributes: 'data-skip-node-update'
          %}
        </nav>
      </div>

      <script
        src="{{ 'header-menu.js' | asset_url }}"
        type="module"
        fetchpriority="low"
      ></script>
    </header-menu>
{% endcase %}

{% style %}
  .header__underlay {
    --color-submenu: rgb({{effective_submenu_color.rgb}});
  }
{% endstyle %}
{% stylesheet %}
  .header__drawer {
    --header-drawer-min-height: 60px;
    display: flex;
    min-height: var(--header-drawer-min-height);
    align-items: center;
  }

  #header-component[data-menu-style='drawer'] .header__drawer {
    display: flex;
    min-height: var(--header-drawer-min-height);
  }

  @media screen and (min-width: 750px) {
    #header-component[data-menu-style='menu'] .header__drawer {
      display: none;
      min-height: 0;
    }
  }

  .header--compact .header__drawer {
    min-height: var(--minimum-touch-target);
  }

  .menu-list--mobile {
    &.menu-list {
      display: grid;
    }

    & .menu-list__list {
      width: max-content;
      margin-inline: auto;
      gap: var(--menu-horizontal-gap);
    }

    & li {
      width: max-content;
      padding-block: var(--padding-sm);
    }

    & li:first-of-type {
      padding-inline-start: var(--menu-horizontal-gap);
    }

    & li:last-of-type {
      padding-inline-end: var(--menu-horizontal-gap);
    }

    & a {
      color: var(--color-foreground);
    }
  }

  .menu-list__scroll-container {
    position: relative;
    overflow-x: auto;
    mask-image: linear-gradient(to right, transparent, #000 20px, #000 calc(100% - 20px), transparent);
    padding-block: var(--padding-2xs);
  }

  .menu-list {
    --menu-horizontal-gap: var(--gap-xl);
    --menu-vertical-gap: var(--gap-xl);

    display: flex;
    height: 100%;
  }

  .menu-list__list {
    display: flex;
    justify-content: var(--grid-area-alignment);
  }

  .menu-list__list-item {
    flex-shrink: 0;
    white-space: nowrap;
    display: flex;
    align-items: center;
    height: 100%;
  }

  .menu-list__list-item[aria-hidden='true'] {
    visibility: hidden;
  }

  .menu-list__link {
    font-family: var(--menu-top-level-font-family);
    font-style: var(--menu-top-level-font-style);
    font-weight: var(--menu-top-level-font-weight);
    font-size: var(--menu-top-level-font-size);
    line-height: var(--menu-top-level-font-line-height);
    text-transform: var(--menu-top-level-font-case);
    color: var(--menu-top-level-font-color);
    text-decoration: none;
    display: flex;
    flex-direction: column;
    justify-content: center;
    cursor: pointer;
    height: 100%;
    margin-block: 0;

    &::after {
      content: '';
      position: absolute;
      display: none;
      top: var(--header-padding);
      height: var(--box-height);
      left: 0;
      right: 0;
    }

    &[data-safety-box='true']::after {
      display: block;
    }

    &:hover,
    &:focus {
      color: var(--menu-top-level-font-color);
    }

    @media screen and (min-width: 750px) {
      font-size: var(--menu-top-level-font-size-desktop);
    }
  }

  /* Solid navbar: reference nav label styles (#171717, 15.4277px Inter) */
  #header-component:not([transparent]) .menu-list,
  #header-component[transparent='not-sticky'][data-sticky-state='active'] .menu-list,
  #header-component[transparent]:has(.menu-list__link:is(:hover, [aria-expanded='true'])) .menu-list {
    --menu-top-level-font-color: #171717;
    --menu-top-level-font-color-rgb: 23 23 23;
  }

  .menu-list__link-title {
    padding-inline: calc(var(--gap-xl) / 2);
  }
  [slot='overflow'] .menu-list__link-title {
    padding-inline: 0;
  }

  .menu-list__list-item:not([slot='overflow']) {
    flex-direction: column;
  }
  .menu-list__list-item[slot='more']:has(.menu-list__link[aria-expanded='true'])::after {
    content: '';
    width: 100%;
    height: var(--header-padding);
    margin-bottom: calc(-1 * var(--header-padding));
  }

  [slot='overflow'] > .menu-list__link::after {
    content: none;
  }

  /*
    High specificity selectors to subdue non-hovered links without javascript.
    If the need for js-generated `hovered` and `focused` classes arises for another reason we can simplify these.
  */
  .menu-list:where(:has(.menu-list__list-item:hover)),
  .menu-list:where(:has(.menu-list__list-item:focus-within)),
  .menu-list:where(:has(.menu-list__list-item:not([aria-hidden='true']) .menu-list__link--active)) {
    .menu-list__link {
      color: rgb(var(--menu-top-level-font-color-rgb) / var(--opacity-subdued-text));
    }
  }

  /* stylelint-disable-next-line selector-max-specificity */
  .menu-list:not(:has(.menu-list__list-item:hover)) .menu-list__link--active,
  .menu-list .menu-list__list-item:where(:hover, :focus-within) .menu-list__link,
  .menu-list .menu-list__list-item:is([slot='more'], [slot='overflow']) .menu-list__link[aria-expanded='true'] {
    color: var(--menu-top-level-font-color);
  }

  .overflow-menu::part(list) {
    /* Make sure focus outline is not cut off by overflow hidden */
    --focus-outline-size: calc(var(--focus-outline-offset) + var(--focus-outline-width));

    gap: 0;
    margin-inline: calc(-1 * var(--menu-horizontal-gap) / 2);
  }

  .overflow-menu {
    /* stylelint-disable-next-line declaration-no-important */
    background-color: transparent !important;
    padding: var(--focus-outline-size);
    margin: calc(-1 * var(--focus-outline-size));
  }

  /** mega menu **/
  .menu-list__submenu,
  .overflow-menu::part(overflow) {
    --submenu-padding-block-start: var(--padding-3xl);
    --submenu-padding-block-end: var(--padding-3xl);

    background-color: transparent;
  }

  .header__row[style*='--border-bottom-width: 0px'] {
    .menu-list__submenu.background-matches-parent,
    .overflow-menu.background-matches-parent::part(overflow) {
      --submenu-padding-block-start: 0px;
    }
  }

  .menu-list__list-item:where(:not([slot='overflow'])) > .menu-list__submenu,
  .overflow-menu::part(overflow) {
    --submenu-content-opacity: 0;
    --submenu-content-animation: opacity calc(var(--submenu-animation-speed) * 0.75) var(--animation-easing);

    visibility: hidden;
    position: absolute;
    width: 100%;
    left: 0;
    top: calc(100% - 1px + var(--border-bottom-width) - (var(--full-open-header-height) - var(--submenu-height)));
    z-index: var(--layer-header-menu);
    padding-inline: var(--padding-inline);
    /* Clip path starts at header height so it doesn't mess with the pointer events in the header */
    clip-path: rect(var(--header-height) 100% var(--full-open-header-height) 0); /* stylelint-disable-line */
    transition: clip-path var(--submenu-animation-speed) var(--ease-out-cubic); /* stylelint-disable-line */
  }

  [data-submenu-overlap-bottom-row] {
    .menu-list__list-item:where(:not([slot='overflow'])) > .menu-list__submenu,
    .overflow-menu::part(overflow) {
      clip-path: rect(var(--top-row-height) 100% var(--full-open-header-height) 0); /* stylelint-disable-line */
    }
  }

  /* Show the submenus on hover */
  .menu-list__list-item:has([aria-expanded='true']) > .menu-list__submenu,
  /* Show the overflow menu when a menu item is hovered */
  .overflow-menu:has([slot="overflow"] [aria-expanded='true'])::part(overflow-list),
  /* Keep the submenus open when they are hovered */
  .menu-list__submenu:is(:hover),
  .overflow-menu::part(overflow):hover {
    --submenu-content-opacity: 1;

    visibility: visible;
  }

  .overflow-menu::part(overflow) {
    --menu-top-level-font-size: var(--font-size--xlarge);

    display: grid;
    grid-template-columns: var(--full-page-grid-with-margins);
  }

  .overflow-menu::part(overflow-list) {
    position: relative;
    display: grid;
    grid-template-columns: minmax(auto, 200px) 1fr;
    grid-template-areas: 'left right';
    grid-template-rows: max-content;
    grid-gap: 0;
    grid-column: 2;
  }

  .menu-list__list-item:is([slot='overflow']) {
    --menu-top-level-font-color: var(--color-foreground);
    --menu-top-level-font-color-rgb: var(--color-foreground-rgb);

    display: contents;
    white-space: normal;

    .menu-list__link {
      grid-area: left;
      grid-row: auto;
      height: min-content;
      font-size: var(--font-size--xl);
      transition: var(--submenu-content-animation);
    }

    .menu-list__submenu {
      visibility: hidden;
      grid-row: 1;
      grid-area: right;
      grid-row-end: span calc(var(--overflow-count) + 1);
      padding-inline-start: var(--menu-horizontal-gap);
    }

    .menu-list__submenu-inner {
      transform: none;
      grid-column: unset;
      padding-block: 0;
    }

    .menu-list__link[aria-expanded='true'] + .menu-list__submenu {
      visibility: visible;
    }
  }

  .header-menu {
    height: 100%;
  }

  .menu-list__submenu-inner {
    position: relative;
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
  }
  
  .menu-list__submenu-inner,
  .overflow-menu::part(overflow-list) {
    padding-block-start: var(--submenu-padding-block-start);
    padding-block-end: var(--submenu-padding-block-end);
    padding-inline: var(--section-padding-inline);
    opacity: var(--submenu-content-opacity);
    transition: var(--submenu-content-animation);
    transform: translateY(calc(var(--full-open-header-height) - var(--submenu-height)));

    /* Make overflow menu scrollable when content exceeds viewport */
    max-height: calc(80vh - var(--header-height));
    overflow-y: auto;
    overflow-x: hidden;
    scrollbar-width: thin;
    scrollbar-color: rgb(var(--color-foreground-rgb) / var(--opacity-40)) transparent;
  }

  .mega-menu__link {
    font-family: var(--menu-child-font-family);
    font-style: var(--menu-child-font-style);
    font-weight: var(--menu-child-font-weight);
    font-size: var(--menu-child-font-size);
    line-height: var(--menu-child-font-line-height);
    text-transform: var(--menu-child-font-case);
    color: var(--menu-child-font-color);
    white-space: normal;
    text-decoration: none;
    display: inline-flex;
    padding: var(--padding-2xs) 0;

    &:hover {
      color: var(--menu-child-active-font-color);
    }
  }

  .mega-menu__link--parent {
    font-family: var(--menu-parent-font-family);
    font-style: var(--menu-parent-font-style);
    font-weight: var(--menu-parent-font-weight);
    font-size: var(--menu-parent-font-size);
    line-height: var(--menu-parent-font-line-height);
    text-transform: var(--menu-parent-font-case);
    color: var(--menu-parent-font-color);

    &:hover {
      color: var(--menu-parent-active-font-color);
    }
  }

  @media screen and (max-width: 989px) {
    .mega-menu__content-list-item--hidden-tablet {
      display: none;
    }
  }

  .mega-menu__link:has(.mega-menu__link-image) {
    display: flex;
    flex-direction: column;
    padding-inline: 0;
    padding-block: var(--padding-sm) 0;
  }

  .mega-menu__link-image {
    width: 100%;
    position: relative;
    aspect-ratio: 16 / 9;
    margin-bottom: var(--padding-sm);
    object-fit: cover;
    border-radius: var(--menu-image-border-radius);
  }

  /* Fix alignment for collection image mode links without images */

  /* Target only top-level links (direct children of column > div) in collection image mode */
  .mega-menu__grid:has(.mega-menu__link-image)
    .mega-menu__column
    > div
    > .mega-menu__link:not(:has(.mega-menu__link-image)) {
    display: flex;
    flex-direction: column;
    padding-inline: 0;
    padding-block: var(--padding-sm) 0;
  }

  .mega-menu__grid:has(.mega-menu__link-image)
    .mega-menu__column
    > div
    > .mega-menu__link:not(:has(.mega-menu__link-image))::before {
    content: '';
    display: block;
    width: 100%;
    aspect-ratio: 16 / 9;
    margin-bottom: var(--padding-sm);
    background-color: var(--color-foreground-muted);
    opacity: 0.1;
    border-radius: var(--menu-image-border-radius);
  }

  .mega-menu__grid {
    display: grid;
    grid-template-columns: repeat(var(--menu-columns-tablet), minmax(0, 1fr));
    gap: var(--menu-vertical-gap) var(--menu-horizontal-gap);
    width: 100%;

    @media screen and (min-width: 990px) {
      grid-template-columns: repeat(var(--menu-columns-desktop), minmax(0, 1fr));
    }
  }

  .mega-menu__column {
    grid-column: span 1;
  }

  .mega-menu__column--span-2 {
    grid-column: span 2;
  }

  .mega-menu__column--span-3 {
    grid-column: span 3;
  }

  .mega-menu__column--span-4 {
    grid-column: span 4;
  }

  .mega-menu__column--wide-collection-image {
    grid-column: span 1;

    @media screen and (min-width: 990px) {
      grid-column: span 2;
    }
  }

  .mega-menu__submenu .mega-menu__column--wide-collection-image {
    grid-column: span 1;
  }

  .mega-menu__content-list {
    display: grid;
    justify-content: end;
    gap: var(--menu-vertical-gap) var(--menu-horizontal-gap);
  }

  .mega-menu__content-list--products {
    grid-template-columns: repeat(var(--menu-content-columns-tablet), minmax(0, 1fr));

    @media screen and (min-width: 990px) {
      grid-template-columns: repeat(var(--menu-content-columns-desktop), minmax(0, 1fr));
    }
  }

  .mega-menu__content-list--collections {
    grid-template-columns: repeat(var(--menu-content-columns-tablet), minmax(0, 300px));

    @media screen and (min-width: 990px) {
      grid-template-columns: repeat(var(--menu-content-columns-desktop), minmax(0, 300px));
    }
  }

  .mega-menu__list {
    display: grid;
    grid-template-columns: subgrid;
    grid-column: span var(--menu-columns-tablet);
    gap: var(--menu-vertical-gap) var(--menu-horizontal-gap);

    @media screen and (min-width: 990px) {
      grid-column: span var(--menu-columns-desktop);
    }
  }

  .mega-menu__content {
    grid-column: span var(--menu-content-columns-tablet) / -1;

    @media screen and (min-width: 990px) {
      grid-column: span var(--menu-content-columns-desktop) / -1;
    }
  }

  .menu-list__list-item[slot='overflow'] .section {
    grid-template-columns: 1fr;
  }

  .menu-list__list-item[slot='overflow'] .section .mega-menu__grid {
    grid-column: 1;
  }

  .mega-menu__content-list li {
    white-space: normal;
  }

  /* mega more menu */
  .mega-menu__more-list {
    --menu-child-font-size: var(--font-size--xl);

    width: 200px;
  }

  .mega-menu__submenu {
    /* preserves the inherited grid layout when this submenu wrapper is used */
    display: contents;
  }

  /* Hide product cards in collapsed submenus from the IntersectionObserver that fires product view events */
  .menu-list__list-item:not(:has([aria-expanded='true'])) > .menu-list__submenu product-component {
    display: none;
  }

  /* Primary nav pills: default label shrinks back; hover surface swipes up with wider padding */
  .menu-list__list-item--nav-pill > .menu-list__link {
    --nav-pill-py: 0.55rem;
    --nav-pill-px: 1.35rem;
    --nav-pill-px-hover: 1.35rem;

    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    border-radius: 999px;
    isolation: isolate;
    background: transparent;
    padding: var(--nav-pill-py) var(--nav-pill-px);
    transition: padding-inline 0.38s var(--ease-out-cubic);
  }

  .menu-list__list-item--nav-pill > .menu-list__link:is(:hover, :focus-visible)::before {
    transform: translateY(0);
  }

  .menu-list__list-item--nav-pill > .menu-list__link:is(:hover, :focus-visible) {
    padding-inline: var(--nav-pill-px-hover);
  }

  .menu-list__list-item--nav-pill > .menu-list__link::before {
    content: attr(data-nav-label);
    position: absolute;
    inset: 0;
    z-index: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: inherit;
    background: #111111;
    color: #ffffff;
    font: inherit;
    letter-spacing: inherit;
    text-transform: inherit;
    white-space: nowrap;
    transform: translateY(100%);
    transition: transform 0.38s var(--ease-out-cubic);
    pointer-events: none;
  }

  .menu-list__list-item--nav-pill > .menu-list__link-title {
    position: relative;
    z-index: 0;
    display: block;
    padding: 0;
    color: var(--menu-top-level-font-color);
    line-height: 1.15;
    transform: scale(1);
    transform-origin: center center;
    opacity: 1;
    transition:
      transform 0.38s var(--ease-out-cubic),
      opacity 0.38s var(--ease-out-cubic);
  }

  .menu-list__list-item--nav-pill > .menu-list__link:is(:hover, :focus-visible) .menu-list__link-title {
    transform: scale(0.86);
    opacity: 0.45;
  }

  @media (prefers-reduced-motion: reduce) {
    .menu-list__list-item--nav-pill > .menu-list__link,
    .menu-list__list-item--nav-pill > .menu-list__link::before,
    .menu-list__list-item--nav-pill > .menu-list__link-title {
      transition: none;
    }

    .menu-list__list-item--nav-pill > .menu-list__link:is(:hover, :focus-visible) .menu-list__link-title {
      transform: none;
      opacity: 1;
    }
  }

  /*
    Shop / Collections panels manage their own padding + entrance motion.
    Theme submenu padding/transform left a ~50–70px grey underlay strip
    below the white rounded panel — zero those here.
  */
  .menu-list__list-item--shop > .menu-list__submenu.shop-mega-menu,
  .menu-list__list-item--collections > .menu-list__submenu.collections-mega-menu {
    --submenu-padding-block-start: 0px;
    --submenu-padding-block-end: 0px;
    --color-submenu: #ffffff;
    background: #ffffff;
    color: #111111;
    border-radius: 0 0 28px 28px;
    box-shadow: 0 24px 64px rgb(0 0 0 / 12%);
    overflow: hidden;
    /* Clip to the panel height (not full-open) so it matches the shortened underlay */
    clip-path: rect(var(--header-height) 100% var(--submenu-height) 0) !important;
  }

  .shop-mega-menu,
  .collections-mega-menu {
    background: #ffffff;
    color: #111111;
    border-radius: 0 0 28px 28px;
    box-shadow: 0 24px 64px rgb(0 0 0 / 12%);
    overflow: hidden;
  }

  .shop-mega-menu .shop-mega-menu__inner {
    display: grid;
    min-height: 465px;
    padding: 2rem max(var(--section-padding-inline), 28px);
    flex-wrap: nowrap;
    transform: none !important;
    max-height: none;
    overflow: visible;
    translate: 0 0;
    opacity: var(--submenu-content-opacity);
    transition: opacity calc(var(--submenu-animation-speed) * 0.75) var(--animation-easing);
  }

  .collections-mega-menu .collections-mega-menu__inner,
  collections-mega-menu.collections-mega-menu__inner {
    display: block;
    min-height: 0;
    flex-wrap: nowrap;
    transform: none !important;
    max-height: none;
    overflow: visible;
    translate: 0 0;
    opacity: var(--submenu-content-opacity);
    transition: opacity calc(var(--submenu-animation-speed) * 0.75) var(--animation-easing);
    box-sizing: border-box;
    /* Clip-path hides the top header-height of this panel — offset so cards aren't cropped */
    padding: calc(var(--header-height, 5rem) + 1.75rem) clamp(2rem, 5vw, 4.5rem) 2.25rem;
  }

  shop-mega-menu.shop-mega-menu__inner,
  .shop-mega-menu .shop-mega-menu__inner {
    display: grid;
    /* Keep sidebar at original share; products absorb the old promo column (2.4fr + 0.95fr) */
    grid-template-columns: minmax(200px, 0.85fr) minmax(0, 3.35fr);
    align-items: stretch;
    gap: clamp(1.75rem, 3.2vw, 3.75rem);
    min-height: min(34rem, calc(100dvh - var(--header-height, 7rem) - 1.25rem));
    height: min(34rem, calc(100dvh - var(--header-height, 7rem) - 1.25rem));
    box-sizing: border-box;
    /* Clip-path hides the top header-height of this panel — offset so labels/cards aren't cropped */
    padding: calc(var(--header-height, 5rem) + 1.75rem) clamp(1.5rem, 3vw, 3rem) 2.5rem;
  }

  .menu-list__list-item--shop:has([aria-expanded='true']) .shop-mega-menu__inner,
  .shop-mega-menu:hover .shop-mega-menu__inner,
  .menu-list__list-item--collections:has([aria-expanded='true']) .collections-mega-menu__inner,
  .collections-mega-menu:hover .collections-mega-menu__inner {
    opacity: 1;
  }

  .shop-mega-menu a,
  .collections-mega-menu a {
    color: inherit;
    text-decoration: none;
  }

  .shop-mega-menu__categories {
    display: flex;
    min-width: 0;
    max-width: 15rem;
    min-height: 100%;
    height: 100%;
    flex-direction: column;
    padding-block: 0;
  }

  .shop-mega-menu__eyebrow {
    flex-shrink: 0;
    margin: 0 0 1.35rem;
    color: #9a9a9a;
    font-size: 0.75rem;
    font-weight: 650;
    letter-spacing: 0.08em;
    line-height: 1.2;
    text-transform: uppercase;
  }

  .shop-mega-menu__category-list {
    display: grid;
    gap: 1.05rem;
    margin: 0;
    padding: 0;
  }

  .shop-mega-menu__category {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    width: 100%;
    padding: 0;
    border: 0;
    background: transparent;
    color: #a0a0a0;
    font: inherit;
    font-size: clamp(1.05rem, 1.45vw, 1.35rem);
    font-weight: 500;
    line-height: 1.15;
    text-align: left;
    text-transform: none;
    cursor: pointer;
    transition: color 180ms ease, translate 180ms ease;
  }

  .shop-mega-menu__category:is(:hover, :focus-visible),
  .shop-mega-menu__category.is-active {
    color: #111111;
    font-weight: 650;
    translate: 2px 0;
  }

  .shop-mega-menu__category:focus-visible {
    outline: 2px solid #111111;
    outline-offset: 4px;
  }

  .shop-mega-menu__category-icon {
    display: inline-flex;
    width: 1.55rem;
    height: 1.55rem;
    flex: 0 0 1.55rem;
    color: currentColor;
  }

  .shop-mega-menu__category-icon svg {
    width: 100%;
    height: 100%;
  }

  .shop-mega-menu__view-all {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
    margin-top: auto;
    padding-top: 1.15rem;
    border-top: 1px solid #d6d6d6;
    font-size: 0.82rem;
    font-weight: 650;
  }

  .shop-mega-menu__view-all span:last-child {
    font-size: 1rem;
    line-height: 1;
  }

  .shop-mega-menu__products {
    display: flex;
    min-width: 0;
    min-height: 100%;
    height: 100%;
    flex-direction: column;
  }

  .shop-mega-menu__products-header {
    display: flex;
    flex-shrink: 0;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
    margin: 0 0 1.35rem;
    color: #9a9a9a;
    font-size: 0.75rem;
    font-weight: 650;
    letter-spacing: 0.08em;
    line-height: 1.2;
    text-transform: uppercase;
  }

  .shop-mega-menu__products-header a {
    display: inline-flex;
    gap: 0.45rem;
    align-items: center;
    color: #111111;
    letter-spacing: -0.01em;
    text-transform: none;
    font-size: 0.875rem;
    font-weight: 650;
  }

  .shop-mega-menu__products-header a strong {
    font-weight: 650;
  }

  /* Shop mega menu entrance — hidden until is-entering (after panel is ~half open) */
  shop-mega-menu:not(.is-entering) .shop-mega-menu__eyebrow,
  shop-mega-menu:not(.is-entering) .shop-mega-menu__products-label,
  shop-mega-menu:not(.is-entering) .shop-mega-menu__products-all-link,
  shop-mega-menu:not(.is-entering) .shop-mega-menu__view-all,
  shop-mega-menu:not(.is-entering) .shop-mega-menu__category-list li,
  shop-mega-menu:not(.is-entering) .shop-mega-menu__product-card {
    opacity: 0;
  }

  @keyframes shop-mega-fade-in {
    from {
      opacity: 0;
    }

    to {
      opacity: 1;
    }
  }

  @keyframes shop-mega-slide-right {
    from {
      opacity: 0;
      transform: translateX(22px);
    }

    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes shop-mega-slide-up {
    from {
      opacity: 0;
      transform: translateY(26px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  shop-mega-menu.is-entering .shop-mega-menu__eyebrow {
    animation: shop-mega-fade-in 0.5s var(--ease-out-cubic) both;
    animation-delay: 0ms;
  }

  shop-mega-menu.is-entering .shop-mega-menu__products-label {
    animation: shop-mega-fade-in 0.5s var(--ease-out-cubic) both;
    animation-delay: 70ms;
  }

  shop-mega-menu.is-entering .shop-mega-menu__products-all-link {
    animation: shop-mega-fade-in 0.5s var(--ease-out-cubic) both;
    animation-delay: 140ms;
  }

  shop-mega-menu.is-entering .shop-mega-menu__view-all {
    animation: shop-mega-fade-in 0.5s var(--ease-out-cubic) both;
    animation-delay: 140ms;
  }

  shop-mega-menu.is-entering .shop-mega-menu__category-list li {
    animation: shop-mega-slide-right 0.52s var(--ease-out-cubic) both;
  }

  shop-mega-menu.is-entering .shop-mega-menu__category-list li:nth-child(1) {
    animation-delay: 90ms;
  }

  shop-mega-menu.is-entering .shop-mega-menu__category-list li:nth-child(2) {
    animation-delay: 150ms;
  }

  shop-mega-menu.is-entering .shop-mega-menu__category-list li:nth-child(3) {
    animation-delay: 210ms;
  }

  shop-mega-menu.is-entering .shop-mega-menu__category-list li:nth-child(4) {
    animation-delay: 270ms;
  }

  shop-mega-menu.is-entering .shop-mega-menu__product-card.is-entering {
    animation: shop-mega-slide-up 0.54s var(--ease-out-cubic) both;
  }

  shop-mega-menu.is-entering .shop-mega-menu__panel.is-active .shop-mega-menu__product-card.is-entering:nth-child(1) {
    animation-delay: 180ms;
  }

  shop-mega-menu.is-entering .shop-mega-menu__panel.is-active .shop-mega-menu__product-card.is-entering:nth-child(2) {
    animation-delay: 260ms;
  }

  shop-mega-menu.is-entering .shop-mega-menu__panel.is-active .shop-mega-menu__product-card.is-entering:nth-child(3) {
    animation-delay: 340ms;
  }

  shop-mega-menu.is-entering .shop-mega-menu__panel.is-active .shop-mega-menu__product-card.is-entering:nth-child(4) {
    animation-delay: 420ms;
  }

  shop-mega-menu.is-entering.is-switching .shop-mega-menu__panel.is-active .shop-mega-menu__product-card.is-entering:nth-child(1) {
    animation-delay: 0ms;
  }

  shop-mega-menu.is-entering.is-switching .shop-mega-menu__panel.is-active .shop-mega-menu__product-card.is-entering:nth-child(2) {
    animation-delay: 75ms;
  }

  shop-mega-menu.is-entering.is-switching .shop-mega-menu__panel.is-active .shop-mega-menu__product-card.is-entering:nth-child(3) {
    animation-delay: 150ms;
  }

  shop-mega-menu.is-entering.is-switching .shop-mega-menu__panel.is-active .shop-mega-menu__product-card.is-entering:nth-child(4) {
    animation-delay: 225ms;
  }

  .shop-mega-menu__panels {
    position: relative;
    flex: 1 1 auto;
    min-height: 0;
  }

  .shop-mega-menu__panel {
    height: 100%;
  }

  .shop-mega-menu__panel[hidden] {
    display: none;
  }

  .shop-mega-menu__product-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 0.85rem;
    align-content: start;
    min-height: 0;
  }

  .shop-mega-menu__product-card {
    display: flex;
    min-width: 0;
    min-height: clamp(16rem, 28vh, 22rem);
    flex-direction: column;
    overflow: hidden;
    border-radius: 18px;
    background: #f4f4f4;
    transition: background 180ms ease, translate 180ms ease;
  }

  .shop-mega-menu__product-card:hover {
    background: #efefef;
    translate: 0 -2px;
  }

  .shop-mega-menu__product-media {
    position: relative;
    display: grid;
    place-items: center;
    flex: 1 1 auto;
    min-height: 0;
    aspect-ratio: 1 / 1.05;
    overflow: visible;
    background: transparent;
  }

  .shop-mega-menu__product-media img,
  .shop-mega-menu__product-media svg {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
    padding: 1rem 0.85rem 0.35rem;
    transition: scale 420ms var(--ease-out-cubic);
  }

  .shop-mega-menu__product-card:hover .shop-mega-menu__product-media img {
    scale: 1.06;
  }

  .shop-mega-menu__product-title {
    position: relative;
    display: inline-block;
    max-width: 100%;
    overflow: hidden;
    font-size: 0.92rem;
    font-weight: 650;
    line-height: 1.2;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .shop-mega-menu__product-title::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: 0;
    width: 100%;
    height: 1.5px;
    background: currentColor;
    transform: scaleX(0);
    transform-origin: left center;
    transition: transform 0.3s ease;
  }

  .shop-mega-menu__product-card:hover .shop-mega-menu__product-title::after {
    transform: scaleX(1);
  }

  .shop-mega-menu__rating {
    position: absolute;
    top: 0.85rem;
    right: 0.85rem;
    z-index: 1;
    display: inline-flex;
    gap: 0.2rem;
    align-items: center;
    padding: 0.15rem 0.35rem;
    border-radius: 999px;
    background: rgb(255 255 255 / 72%);
    font-size: 0.62rem;
    font-weight: 600;
    line-height: 1;
  }

  .shop-mega-menu__rating > span {
    color: #e9a21a;
  }

  .shop-mega-menu__product-copy {
    display: grid;
    gap: 0.2rem;
    padding: 0.15rem 0.95rem 1rem;
    white-space: normal;
  }

  .shop-mega-menu__vendor {
    overflow: hidden;
    color: #9a9a9a;
    font-size: 0.58rem;
    letter-spacing: 0.1em;
    text-overflow: ellipsis;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .shop-mega-menu__product-price {
    color: #222222;
    font-size: 0.78rem;
    line-height: 1.3;
  }

  .shop-mega-menu__swatches {
    display: flex;
    flex-wrap: wrap;
    gap: 0.3rem;
    align-items: center;
    min-height: 1.1rem;
    margin-top: 0.45rem;
  }

  .shop-mega-menu__swatch {
    display: inline-flex;
    width: 1.05rem;
    height: 1.05rem;
    overflow: hidden;
    border: 1px solid rgb(0 0 0 / 8%);
    border-radius: 50%;
    background: #ffffff;
  }

  .shop-mega-menu__swatch img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .shop-mega-menu__swatch-more {
    color: #777777;
    font-size: 0.62rem;
    line-height: 1;
  }

  @media (prefers-reduced-motion: reduce) {
    shop-mega-menu:not(.is-entering) .shop-mega-menu__eyebrow,
    shop-mega-menu:not(.is-entering) .shop-mega-menu__products-label,
    shop-mega-menu:not(.is-entering) .shop-mega-menu__products-all-link,
    shop-mega-menu:not(.is-entering) .shop-mega-menu__category-list li,
    shop-mega-menu:not(.is-entering) .shop-mega-menu__product-card {
      opacity: 1;
    }

    shop-mega-menu.is-entering .shop-mega-menu__eyebrow,
    shop-mega-menu.is-entering .shop-mega-menu__products-label,
    shop-mega-menu.is-entering .shop-mega-menu__products-all-link,
    shop-mega-menu.is-entering .shop-mega-menu__view-all,
    shop-mega-menu.is-entering .shop-mega-menu__category-list li,
    shop-mega-menu.is-entering .shop-mega-menu__product-card.is-entering {
      animation: none;
      opacity: 1;
      transform: none;
    }
  }

  .collections-mega-menu__grid {
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: clamp(0.85rem, 1.5vw, 1.25rem);
    width: 100%;
  }

  .collections-mega-menu__card {
    position: relative;
    display: flex;
    aspect-ratio: 3 / 4.15;
    min-height: clamp(18rem, 36vh, 24rem);
    flex-direction: column;
    overflow: hidden;
    border-radius: 20px;
    background: #f5f5f5;
    box-shadow: none;
    white-space: normal;
    color: #111111;
    transition: translate 220ms ease, box-shadow 220ms ease;
  }

  collections-mega-menu:not(.is-active) .collections-mega-menu__card {
    opacity: 0;
  }

  .collections-mega-menu__card.is-entering {
    animation: collections-mega-card-enter 0.54s var(--ease-out-cubic) both;
  }

  collections-mega-menu.is-active .collections-mega-menu__card.is-entering:nth-child(1) {
    animation-delay: 0ms;
  }

  collections-mega-menu.is-active .collections-mega-menu__card.is-entering:nth-child(2) {
    animation-delay: 80ms;
  }

  collections-mega-menu.is-active .collections-mega-menu__card.is-entering:nth-child(3) {
    animation-delay: 160ms;
  }

  collections-mega-menu.is-active .collections-mega-menu__card.is-entering:nth-child(4) {
    animation-delay: 240ms;
  }

  collections-mega-menu.is-active .collections-mega-menu__card.is-entering:nth-child(5) {
    animation-delay: 320ms;
  }

  @keyframes collections-mega-card-enter {
    from {
      opacity: 0;
      transform: translateY(26px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .collections-mega-menu__card:hover {
    translate: 0 -3px;
    box-shadow: 0 12px 28px rgb(0 0 0 / 0.08);
  }

  .collections-mega-menu__card-media {
    position: relative;
    flex: 7 0 0;
    min-height: 0;
    overflow: hidden;
    padding: 0;
    background: #f5f5f5;
  }

  .collections-mega-menu__card-media img,
  .collections-mega-menu__card-media svg {
    display: block;
    width: 100%;
    height: 100%;
    max-height: none;
    object-fit: contain;
    object-position: center center;
    transition: scale 420ms var(--ease-out-cubic);
  }

  .collections-mega-menu__card:hover .collections-mega-menu__card-media img {
    scale: 1.03;
  }

  .collections-mega-menu__card-footer {
    position: relative;
    z-index: 1;
    display: flex;
    flex: 3 0 0;
    align-items: flex-end;
    justify-content: space-between;
    gap: 0.75rem;
    min-height: 0;
    padding: 0.85rem 1.5rem 1.2rem;
  }

  .collections-mega-menu__card.is-entering .collections-mega-menu__card-footer {
    animation: collections-mega-card-copy-enter 0.46s var(--ease-out-cubic) both;
  }

  collections-mega-menu.is-active .collections-mega-menu__card.is-entering:nth-child(1) .collections-mega-menu__card-footer {
    animation-delay: 200ms;
  }

  collections-mega-menu.is-active .collections-mega-menu__card.is-entering:nth-child(2) .collections-mega-menu__card-footer {
    animation-delay: 280ms;
  }

  collections-mega-menu.is-active .collections-mega-menu__card.is-entering:nth-child(3) .collections-mega-menu__card-footer {
    animation-delay: 360ms;
  }

  collections-mega-menu.is-active .collections-mega-menu__card.is-entering:nth-child(4) .collections-mega-menu__card-footer {
    animation-delay: 440ms;
  }

  collections-mega-menu.is-active .collections-mega-menu__card.is-entering:nth-child(5) .collections-mega-menu__card-footer {
    animation-delay: 520ms;
  }

  @keyframes collections-mega-card-copy-enter {
    from {
      opacity: 0;
      transform: translateX(18px);
    }

    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  .collections-mega-menu__card-copy {
    display: grid;
    gap: 0.3rem;
    min-width: 0;
  }

  .collections-mega-menu__card-copy strong {
    font-size: clamp(1rem, 1.35vw, 1.2rem);
    font-weight: 700;
    line-height: 1.15;
    letter-spacing: -0.015em;
    text-transform: none;
  }

  .collections-mega-menu__card-copy small {
    color: #6b6b6b;
    font-size: 0.875rem;
    font-weight: 400;
    line-height: 1.35;
  }

  .collections-mega-menu__card-arrow {
    flex: none;
    align-self: flex-end;
    margin-block-end: 0.15rem;
    font-size: 1.05rem;
    line-height: 1;
    color: inherit;
  }

  .collections-mega-menu__card--featured {
    display: block;
    color: #ffffff;
    background: #2a2a2a;
    box-shadow: none;
  }

  .collections-mega-menu__card--featured .collections-mega-menu__card-media {
    position: absolute;
    inset: 0;
    flex: none;
    padding: 0;
    background: #2a2a2a;
  }

  .collections-mega-menu__card--featured .collections-mega-menu__card-media img,
  .collections-mega-menu__card--featured .collections-mega-menu__card-media svg {
    object-fit: cover;
    object-position: center center;
  }

  .collections-mega-menu__card-shade {
    position: absolute;
    inset: 52% 0 0;
    z-index: 1;
    background: linear-gradient(
      to top,
      rgb(0 0 0 / 62%) 0%,
      rgb(0 0 0 / 28%) 42%,
      transparent 100%
    );
    pointer-events: none;
  }

  .collections-mega-menu__card--featured .collections-mega-menu__card-footer {
    position: absolute;
    inset: auto 0 0;
    z-index: 2;
    flex: none;
    align-items: flex-end;
    padding: 1rem 1.5rem 1.25rem;
  }

  .collections-mega-menu__card--featured .collections-mega-menu__card-copy strong {
    font-size: clamp(1.1rem, 1.4vw, 1.35rem);
    font-weight: 700;
    color: #ffffff;
  }

  .collections-mega-menu__card--featured .collections-mega-menu__card-copy small {
    color: #ffffff;
    font-size: 0.875rem;
  }

  @media (prefers-reduced-motion: reduce) {
    collections-mega-menu:not(.is-active) .collections-mega-menu__card {
      opacity: 1;
    }

    .collections-mega-menu__card.is-entering,
    .collections-mega-menu__card.is-entering .collections-mega-menu__card-footer {
      animation: none;
      opacity: 1;
      transform: none;
    }
  }

  @media screen and (max-width: 1100px) {
    shop-mega-menu.shop-mega-menu__inner,
    .shop-mega-menu .shop-mega-menu__inner {
      grid-template-columns: minmax(160px, 0.8fr) minmax(0, 2.95fr);
      gap: 1.25rem;
      min-height: min(30rem, calc(100dvh - var(--header-height, 7rem) - 1rem));
      height: min(30rem, calc(100dvh - var(--header-height, 7rem) - 1rem));
      padding: calc(var(--header-height, 5rem) + 1.5rem) 1.5rem 2rem;
    }

    .shop-mega-menu__category {
      font-size: 0.95rem;
    }

    .shop-mega-menu__product-title {
      font-size: 0.82rem;
    }

    .collections-mega-menu .collections-mega-menu__inner {
      padding-inline: 1.5rem;
      padding-bottom: 2rem;
    }

    .collections-mega-menu__grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .collections-mega-menu__card {
      min-height: 18rem;
    }
  }

  @media screen and (max-width: 749px) {
    .collections-mega-menu .collections-mega-menu__inner {
      padding-inline: 1rem;
    }

    .collections-mega-menu__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:names.menu",
  "tag": null,
  "settings": [
    {
      "type": "link_list",
      "id": "menu",
      "label": "t:settings.menu",
      "default": "main-menu"
    },
    {
      "type": "header",
      "content": "Shop dropdown"
    },
    {
      "type": "checkbox",
      "id": "show_shop_dropdown",
      "label": "Show Shop dropdown",
      "default": true
    },
    {
      "type": "collection",
      "id": "mega_category_1",
      "label": "Category 1",
      "info": "Used in Shop and Collections dropdowns"
    },
    {
      "type": "collection",
      "id": "mega_category_2",
      "label": "Category 2"
    },
    {
      "type": "collection",
      "id": "mega_category_3",
      "label": "Category 3"
    },
    {
      "type": "collection",
      "id": "mega_category_4",
      "label": "Category 4"
    },
    {
      "type": "product_list",
      "id": "mega_products",
      "label": "Popular products",
      "limit": 4
    },
    {
      "type": "image_picker",
      "id": "mega_promo_image",
      "label": "Promo image"
    },
    {
      "type": "text",
      "id": "mega_promo_title",
      "label": "Promo heading",
      "default": "Build your bundle"
    },
    {
      "type": "text",
      "id": "mega_promo_text",
      "label": "Promo text",
      "default": "Buy 3 and save 30%"
    },
    {
      "type": "url",
      "id": "mega_promo_link",
      "label": "Promo link"
    },
    {
      "type": "header",
      "content": "Collections dropdown"
    },
    {
      "type": "checkbox",
      "id": "show_collections_dropdown",
      "label": "Show Collections dropdown",
      "default": true
    },
    {
      "type": "text",
      "id": "collections_card_description_1",
      "label": "Category 1 description",
      "default": "Surround yourself in sound."
    },
    {
      "type": "text",
      "id": "collections_card_description_2",
      "label": "Category 2 description",
      "default": "Small size, big sound."
    },
    {
      "type": "text",
      "id": "collections_card_description_3",
      "label": "Category 3 description",
      "default": "Fill the room with music."
    },
    {
      "type": "text",
      "id": "collections_card_description_4",
      "label": "Category 4 description",
      "default": "Complete your setup."
    },
    {
      "type": "image_picker",
      "id": "collections_all_image",
      "label": "All Collections image"
    },
    {
      "type": "text",
      "id": "collections_all_title",
      "label": "All Collections heading",
      "default": "All Collections"
    },
    {
      "type": "text",
      "id": "collections_all_text",
      "label": "All Collections text",
      "default": "Check out all our collections."
    },
    {
      "type": "url",
      "id": "collections_all_link",
      "label": "All Collections link"
    },
    {
      "type": "header",
      "content": "t:content.appearance"
    },
    {
      "type": "paragraph",
      "content": "t:content.menu_appearance_paragraph"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "t:settings.background_color",
      "default": "{{ settings.color_palette.background }}"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "t:settings.text_color",
      "placeholder": "t:settings.default"
    },
    {
      "type": "header",
      "content": "t:content.typography"
    },
    {
      "type": "select",
      "id": "type_font_primary_size",
      "label": "t:settings.top_level_size",
      "options": [
        {
          "value": "0.625rem",
          "label": "10px"
        },
        {
          "value": "0.75rem",
          "label": "12px"
        },
        {
          "value": "0.875rem",
          "label": "14px"
        },
        {
          "value": "1rem",
          "label": "16px"
        },
        {
          "value": "1.125rem",
          "label": "18px"
        }
      ],
      "default": "1rem"
    },
    {
      "type": "select",
      "id": "menu_font_style",
      "label": "t:settings.submenu_size",
      "options": [
        {
          "value": "regular",
          "label": "t:options.small"
        },
        {
          "value": "inverse",
          "label": "t:options.medium"
        },
        {
          "value": "inverse_large",
          "label": "t:options.large"
        }
      ],
      "default": "inverse"
    },
    {
      "type": "select",
      "id": "type_font_primary_link",
      "label": "t:settings.font",
      "options": [
        {
          "value": "body",
          "label": "t:options.body"
        },
        {
          "value": "subheading",
          "label": "t:options.subheading"
        },
        {
          "value": "heading",
          "label": "t:options.heading"
        },
        {
          "value": "accent",
          "label": "t:options.accent"
        }
      ],
      "default": "heading"
    },
    {
      "type": "select",
      "id": "type_case_primary_link",
      "label": "t:settings.text_case",
      "options": [
        {
          "value": "none",
          "label": "t:options.default"
        },
        {
          "value": "uppercase",
          "label": "t:options.uppercase"
        }
      ],
      "default": "none"
    },
    {
      "type": "header",
      "content": "t:content.submenu_feature"
    },
    {
      "type": "select",
      "id": "menu_style",
      "label": "t:settings.media_type",
      "info": "t:info.media_type_info",
      "options": [
        {
          "value": "text",
          "label": "t:options.none"
        },
        {
          "value": "collection_images",
          "label": "t:options.collection_images"
        },
        {
          "value": "featured_products",
          "label": "t:options.featured_products"
        },
        {
          "value": "featured_collections",
          "label": "t:options.featured_collections"
        }
      ],
      "default": "collection_images"
    },
    {
      "type": "select",
      "id": "featured_products_aspect_ratio",
      "label": "t:settings.image_ratio",
      "options": [
        {
          "value": "adapt",
          "label": "t:options.auto"
        },
        {
          "value": "4 / 5",
          "label": "t:options.portrait"
        },
        {
          "value": "1 / 1",
          "label": "t:options.square"
        }
      ],
      "default": "4 / 5",
      "visible_if": "{{ block.settings.menu_style == 'featured_products' }}"
    },
    {
      "type": "select",
      "id": "featured_collections_aspect_ratio",
      "label": "t:settings.image_ratio",
      "options": [
        {
          "value": "adapt",
          "label": "t:options.auto"
        },
        {
          "value": "16 / 9",
          "label": "t:options.landscape"
        },
        {
          "value": "1 / 1",
          "label": "t:options.square"
        }
      ],
      "default": "16 / 9",
      "visible_if": "{{ block.settings.menu_style == 'featured_collections' }}"
    },
    {
      "type": "range",
      "id": "image_border_radius",
      "label": "t:settings.image_border_radius",
      "min": 0,
      "max": 32,
      "step": 1,
      "unit": "px",
      "default": 0,
      "visible_if": "{{ block.settings.menu_style != 'text' }}"
    },
    {
      "type": "header",
      "content": "t:content.mobile_layout"
    },
    {
      "type": "checkbox",
      "id": "navigation_bar",
      "label": "t:settings.navigation_bar"
    },
    {
      "type": "color",
      "id": "background_color_navigation_bar",
      "label": "t:settings.navigation_bar_background_color",
      "default": "{{ settings.color_palette.background }}",
      "visible_if": "{{ block.settings.navigation_bar }}"
    },
    {
      "type": "color",
      "id": "text_color_navigation_bar",
      "label": "t:settings.navigation_bar_text_color",
      "placeholder": "t:settings.default",
      "visible_if": "{{ block.settings.navigation_bar }}"
    },
    {
      "type": "checkbox",
      "id": "drawer_accordion",
      "label": "t:settings.accordion",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "drawer_accordion_expand_first",
      "label": "t:settings.expand_first_group",
      "default": false,
      "visible_if": "{{ block.settings.drawer_accordion == true }}"
    },
    {
      "type": "checkbox",
      "id": "drawer_dividers",
      "label": "t:settings.dividers",
      "default": false
    }
  ]
}
{% endschema %}
